"""
Example applications for the GigQ library.

Examples print progress and results only. For GigQ's internal job/worker logs,
call ``setup_logging()`` from ``gigq`` in your script (see each example's docstring).
"""
